Core modules which handle maptheme's internal logic & external interface.

The system modules, NixOS and Darwin, will load the Home Manager module as well via the `home-manager.sharedModules` attribute.
